# Distribution Outputs

This folder separates build outputs by release channel.

```text
dist/github/
  GitHub Release and direct-distribution files, such as .dmg and .pkg installers.

dist/appstore/
  Mac App Store upload packages generated by Scripts/package-appstore.sh.
```

Installer binaries are intentionally ignored by git. Keep only this folder structure and its notes in the repository.
